// import Router from 'path/to/Router';

/**
 *  怎样用TS直接实例化? 而不是一个类
 */

export default {

    init(app: object) {
        // app.router = new Router(app.options.router);
        // app.router.to('home');

    }

};